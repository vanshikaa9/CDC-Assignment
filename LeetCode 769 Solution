//SOLUTION 1

class Solution {
    public int maxChunksToSorted(int[] a) {
        int m = 0, c = 0;
        for (int i = 0; i < a.length; i++) {
            m = Math.max(m, a[i]);
            if (m == i) c++;
        }
        return c;
    }
}
