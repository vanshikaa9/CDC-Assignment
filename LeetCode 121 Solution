//SOLUTION 1

class Solution {
    public int maxProfit(int[] a) {
        int min=a[0],ans=0;
        for(int i=1;i<a.length;i++){
            if(a[i]<min)min=a[i];
            else ans=Math.max(ans,a[i]-min);
        }
        return ans;
    }
}
