//SOLUTION 1

class Solution {
    public int minCostClimbingStairs(int[] a) {
        int n = a.length;
        for (int i = 2; i < n; i++)
            a[i] += Math.min(a[i - 1], a[i - 2]);
        return Math.min(a[n - 1], a[n - 2]);
    }
}
