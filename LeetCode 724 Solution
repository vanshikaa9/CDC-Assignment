//SOLUTION 1


class Solution
{
    public int pivotIndex(int[] a)
    {
        int n=a.length,s=0,l=0;
        for(int i=0;i<n;i++)
        s+=a[i];
        for(int i=0;i<n;i++)
        {
            if(l==s-l-a[i])
            return i;
            l+=a[i];
        }
        return -1;
    }
}
